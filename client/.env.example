# API base URL (no trailing slash).
# Local: http://localhost:4000
# Production (Vercel): leave unset/empty so the browser uses same-origin /api
# (proxied to Render via vercel.json). Do NOT point this at *.onrender.com —
# cross-site cookies break SSO + refresh.
VITE_API_URL=http://localhost:4000
